[project]
name = "social-media-video-downloader"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "yt-dlp",
    "python-multipart",
    "python-dotenv"
]
